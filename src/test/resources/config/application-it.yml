customerLoginRealm:
  testUser:  ${CUSTOMER_LOGIN_TEST_USER:test}
  testPassword:  ${CUSTOMER_LOGIN_TEST_PASSWORD:test}
  testCustomerNumber:  ${CUSTOMER_LOGIN_TEST_CUSTOMER_NUMBER:12345}

tokens:
    test-tokens: customerLogin=1234567889
    accessTokenUri: http://localhost:44332
    credentialsDirectory: .

cassandra:
  keyspace: provider
  contactPoints: 127.0.0.1
  clusterName: Test Cluster

logging:
  level:
    # print the AutoConfiguration report
    org.springframework.boot.autoconfigure.logging.AutoConfigurationReportLoggingInitializer: DEBUG
